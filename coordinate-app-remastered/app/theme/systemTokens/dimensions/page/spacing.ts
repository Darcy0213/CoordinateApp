import { dimensions } from '@theme/referenceTokens/dimensions'
import { PageSpacing as type } from '@theme/types/theme'

const PageSpacing = (): type => {
	return {
		default: dimensions[12]
	}
}

export default PageSpacing
